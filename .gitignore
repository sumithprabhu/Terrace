node_modules/
website/.next/

# electron-builder packaged output
release/

# Local reference copy of the v0 template the landing page was adapted from,
# not part of the shipped site
homie-template/

# Local-only demo recording tooling (simulated peers, scripted walkthrough)
demo/

# Hypercore append-only log storage (per-peer, local only)
rooms/
terrace-storage/

# Electron userData (identity keys, per-instance profiles)
.terrace-data/

# OS/editor cruft
.DS_Store
*.log
